from tkinter import * 

root = Tk()


titre = Label(root,text="Download Organiser")
description = Label(root,text="Script python qui organise le fichier de telechargement")


titre.grid(row=0,column=0)
description.grid(row=1,column=0)

root.mainloop()